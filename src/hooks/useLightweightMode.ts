/**
 * Re-export lightweight mode hooks for backward compatibility
 * with the architecture's expected hook locations.
 */
export { useIsLightweight, useLightweightMode } from "@/contexts/LightweightContext";
